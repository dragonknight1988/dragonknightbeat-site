name: 🚗 每日汽车新闻

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

concurrency:
  group: daily-content
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: gh-pages
          fetch-depth: 0
      - name: 📂 从 main 取脚本
        run: |
          git fetch origin main
          git checkout origin/main -- scripts/
      - name: 🐍 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: 📦 安装依赖
        run: pip install requests
      - name: 🤖 生成
        run: python3 scripts/generate_daily-auto.py --output daily-auto.json
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      - name: 🚀 提交并推送
        run: |
          git config user.name "dragonknightbot"
          git config user.email "bot@dragonknightbeat.com"
          git add daily-auto.json
          if git diff --staged --quiet; then
            echo "⚠️ 无变更"
          else
            git commit -m "auto: daily-auto $(date +%Y-%m-%d)"
            git push origin gh-pages
            echo "✅ 已推送"
          fi
